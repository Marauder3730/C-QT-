# 开发心得体会

## 项目背景

本项目是面向对象程序设计（OOP）课程设计，题目为「公司员工管理系统」。要求实现三类员工（工程师、领导、主任工程师）的输入、输出、修改、删除，并体现封装、继承、多态等面向对象特性。

## 技术选型

- **语言**：C++11
- **界面框架**：Qt 5.12（Widgets + Charts）
- **数据持久化**：JSON 文件（`employee.json`）
- **开发工具**：Qt Creator

选择 Qt 的原因：课程要求图形界面，Qt 的信号槽机制便于界面与业务分离，JSON 读写也有成熟 API。

## 架构设计

采用三层结构：

1. **实体层**：`Employee` 抽象基类 + 三个派生类
2. **业务层**：`EmployeeManager` 负责 CRUD、统计、组织关系同步、文件读写
3. **界面层**：`MainWindow`、`AddEditDialog`、`LoginDialog`、`StatDialog`

界面不直接操作数据结构，统一通过 `EmployeeManager` 调用，便于维护和答辩讲解。

## 遇到的困难与解决

### 1. 多态与指针管理

一开始不太理解为什么要用 `Employee*` 存不同类型员工。后来发现在算平均工资、刷新表格时，只需写 `emp->calcSalary()`，不用写大量 `if-else`，才真正体会到多态的价值。

### 2. 领导与下属的关联

工程师需要选择直属领导，领导的下属人数又不能手填。最终采用：工程师存 `leaderId`，每次增删改后由 `syncRelationships()` 自动统计并更新领导的 `subordinateCount`。

### 3. Qt 5.12 兼容性

部分新写法（如 `QOverload`、Charts 命名空间）在 5.12 下报错，查阅文档后改用兼容写法。

### 4. 界面体验

对话框过高导致按钮被挡住、深色主题下表单文字看不清等问题，通过固定对话框高度、滚动区域、表单区浅色背景等方式逐步调整。

## 收获

- 更理解 **封装**（私有成员 + getter/setter）、**继承**（公共属性放基类）、**多态**（虚函数 + 基类指针）
- 学会用 **分层思想** 组织代码，而不是把所有逻辑堆在界面里
- 熟悉了 Qt 的基本控件、对话框、表格、图表和 JSON 文件操作
- 完成一个能演示、能答辩、能开源的完整小项目

## 若继续改进

- [ ] 把登录账号密码移到配置文件
- [ ] 增加单元测试
- [ ] 支持导出 Excel
- [ ] 升级到 Qt 6

---

*作者：汪昳娴 · OOP 课程设计*
